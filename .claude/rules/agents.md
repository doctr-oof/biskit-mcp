# Custom Agents
This repo has a custom `Explore` agent (located in `.claude/agents/Explore.md`). When you need to explore complex systems (or multiple systems of various complexity), use it to speed up exploration and context gathering - even if you aren't in Plan Mode.
